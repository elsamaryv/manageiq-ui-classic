- if @ae_class
  - if @in_a_form
    = react('MiqAeClass',
      :classRecord => @ae_class,
      :fqname => @sb[:namespace_path])
-# if @ae_class && !@in_a_form
-#  = miq_ae_customization_summary(@ae_class)
